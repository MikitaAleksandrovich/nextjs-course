import { useRouterHook } from "next/router";

const PortfolioProjectPage = () => {
  const router = useRouter();

  return (
    <div>
      <h1>This is Portfolio Project Page</h1>
    </div>
  );
};

export default PortfolioProjectPage;
